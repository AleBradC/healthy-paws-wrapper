
# Values here MUST match the same names in healthy-paws-service/.env or
# the `db` and `backend` services will disagree.

# --- Postgres ---------------------------------------------------------
DB_USER=healthy_paws
DB_PASSWORD=change-me-strong-password
DB_DATABASE=healthy_paws
